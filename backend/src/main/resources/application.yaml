spring:
  application:
    name: rfp

  # 1. Database Connection
  datasource:
    url: jdbc:postgresql://localhost:5432/airfp_system?autoReconnect=true
    username: airfp_user
    password: airfp_password
    driver-class-name: org.postgresql.Driver

  # 2. JPA / Hibernate (Aligned with datasource, not inside it)
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  # 3. Flyway (Aligned with datasource)
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  mail:
    host: sandbox.smtp.mailtrap.io
    port: 587
    username: 3484f183fa89d3
    password: 8ae6c464fc5c60
    properties:
      mail:
        debug: true #remove
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

logging:
  level:
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.mail: DEBUG
    com.sun.mail: DEBUG

perplexity:
  api:
    key: ${PERPLEXITY_API_KEY:}
    model: sonar-pro
    base-url: https://api.perplexity.ai
    timeout-seconds: 30

app:
  mail:
    from: akashlnetwin@yahoo.com